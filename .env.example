# ===============================================
# DATABASE CONFIGURATION
# ===============================================
# PostgreSQL connection string for Prisma
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# 
# Example local development:
#   postgresql://postgres:your_password@localhost:5432/eterna_vida_dev?schema=public
#
# Example production (Neon):
#   postgresql://user:password@ep-xxxxx.us-east-2.aws.neon.tech/eterna_vida?schema=public&sslmode=require
#
# IMPORTANT:
# - This value is SERVER-ONLY (never exposed to client)
# - Validated in src/config/env.ts with Zod
# - Read via environment variable on server startup
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/eterna_vida?schema=public"

# ===============================================
# RUNTIME ENVIRONMENT
# ===============================================
# Valid values: development | test | production
NODE_ENV="development"

# ===============================================
# PUBLIC ACCOUNT AUTHENTICATION (CLERK)
# ===============================================
# Public storefront login/register use Clerk with email + password.
# These keys are required to enable /login and /register.
# No social providers are enabled by default.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_replace_me"
CLERK_SECRET_KEY="sk_test_replace_me"

# ===============================================
# INTERNAL ADMIN AUTHORIZATION
# ===============================================
# Server-side credentials used by the admin login endpoint.
# The session secret signs the httpOnly cookie for protected admin API access.
ADMIN_AUTH_EMAIL="admin@eternavida.com.ec"
ADMIN_AUTH_PASSWORD="change-this-admin-password"
STAFF_AUTH_EMAIL="staff@eternavida.com.ec"
STAFF_AUTH_PASSWORD="change-this-staff-password"
AUTH_SESSION_SECRET="change-this-session-secret-with-at-least-32-characters"
AUTH_SESSION_MAX_AGE_SECONDS="28800"

# ===============================================
# RESEND — TRANSACTIONAL EMAIL
# ===============================================
RESEND_API_KEY="re_replace_me"
EMAIL_FROM_NAME="Eterna Vida"
EMAIL_FROM_ADDRESS="noreply@eternavida.com.ec"
EMAIL_REPLY_TO="info@eternavida.com.ec"

